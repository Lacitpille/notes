---
import { url } from '../lib/url';
---

<header class="border-b border-gray-200 mb-8">
  <div class="max-w-2xl mx-auto px-4 py-4 flex items-center justify-between">
    <a href={url('/')} class="text-lg font-semibold text-gray-900 no-underline hover:text-gray-600">
      Math Notes
    </a>
    <nav class="flex gap-6 text-sm">
      <a href={url('/posts/')} class="text-gray-600 hover:text-gray-900">Posts</a>
      <a href={url('/tags/')} class="text-gray-600 hover:text-gray-900">Tags</a>
    </nav>
  </div>
</header>
