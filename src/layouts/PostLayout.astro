---
import BaseLayout from './BaseLayout.astro';
import { url } from '../lib/url';

interface Props {
  title: string;
  pubDate: Date;
  tags: string[];
  description?: string;
}

const { title, pubDate, tags, description } = Astro.props;
const dateStr = pubDate.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' });
---

<BaseLayout title={title} description={description}>
  <div class="max-w-2xl mx-auto px-4 py-10">
    <header class="mb-10">
      <div class="flex flex-wrap gap-2 mb-3">
        {tags.map(tag => (
          <a href={url('/tags/' + tag + '/')} class="text-xs bg-indigo-50 text-indigo-600 px-2.5 py-0.5 rounded-full font-medium hover:bg-indigo-100 transition-colors no-underline">
            {tag}
          </a>
        ))}
      </div>
      <h1 class="text-3xl font-bold text-gray-900 leading-tight">{title}</h1>
      <time class="mt-3 block text-sm text-gray-400">{dateStr}</time>
      <div class="mt-6 h-px bg-gradient-to-r from-indigo-200 via-indigo-100 to-transparent"></div>
    </header>
    <article class="prose prose-slate lg:prose-lg max-w-none prose-headings:text-gray-900 prose-a:text-indigo-600 prose-a:no-underline hover:prose-a:underline">
      <slot />
    </article>
  </div>
</BaseLayout>
